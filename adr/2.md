7/18/2026 

Implement internal memtable as skiplist. Wrap in DB that exposes ops to client

### External Contract
1. Put(key, value) 
    - takes key values as []byte, stores ordered in mem table
1. Delete(key)
    - stores delete operation for key

### Black Boxing
LSM -> DB -> Memtable -> return value

### Invariants
1. Records must be sorted by key, then sequence number
1. Sequence number must increase for every mutation recorded

### Build Sequence
1. Implement internal memtable
1. Implement external contract through DB